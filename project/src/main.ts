/**
 * Convert roman number to int
 * 
 * @param roman  roman number
 * @returns      number 
 */
export function romantoInt(roman:string): number {
    return 0
}

/**
 * Return winner player based on Score object.
 * 
 * @param value  score object.
 * @returns      winner player name ('Player 1', 'Player 2'). Return 'Deuce' if score is equal. Return 'Advantage Player 1', 'Advantage Player 2' if score difference is equal to 1 and one of player get score more than 4.
 */
export function intToRoman(value:number): string {
    return ''
}


/**
 * add 2 roman numbers
 * 
 * @param roman1  roman number
 * @param roman2  roman number
 * @returns      winner player name ('Player 1', 'Player 2'). Return 'Deuce' if score is equal. Return 'Advantage Player 1', 'Advantage Player 2' if score difference is equal to 1 and one of player get score more than 4.
 */
export function addRomanNumbers(roman1:string, roman2:string): string {
    return ''
}
